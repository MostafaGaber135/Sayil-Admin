info:
  name: Get Role By Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/admin/roles/{id}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
