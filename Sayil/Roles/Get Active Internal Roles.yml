info:
  name: Get Active Internal Roles
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/admin/roles/active-internal"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
