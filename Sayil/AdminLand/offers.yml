info:
  name: offers
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/admin/land/{landId}/offers"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
