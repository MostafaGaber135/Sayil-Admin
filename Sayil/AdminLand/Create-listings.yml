info:
  name: Create-listings
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/listings"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
