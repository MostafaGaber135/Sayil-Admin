info:
  name: update-classification
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/update-classification"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
