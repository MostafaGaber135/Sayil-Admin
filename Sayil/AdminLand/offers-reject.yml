info:
  name: offers/reject
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/offers/reject"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
