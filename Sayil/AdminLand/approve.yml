info:
  name: approve
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/approve"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
