info:
  name: price-change-request
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/price-change-request"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
