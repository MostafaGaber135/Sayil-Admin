info:
  name: land/upload-images
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/upload-images"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
