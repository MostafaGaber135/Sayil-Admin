info:
  name: land/add
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/add"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
