info:
  name: Delete-Land
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/land/{id}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
