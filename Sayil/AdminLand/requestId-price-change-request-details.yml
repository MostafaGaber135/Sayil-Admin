info:
  name: requestId/price-change-request-details
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/api/admin/land/listings/{requestId}/price-change-request-details"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
