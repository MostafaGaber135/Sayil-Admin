info:
  name: land/upload-document
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/upload-document"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
