info:
  name: reject
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/admin/land/reject"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
