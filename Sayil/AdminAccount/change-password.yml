info:
  name: change-password
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/admin/account/change-password"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
