info:
  name: AdminLookup-All
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/admin/lookup/all"
  headers:
    - name: Authorization
      value: "{{token]}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
