info:
  name: change-password
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/admin/auth/change-password"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
